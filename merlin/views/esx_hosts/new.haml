.block
  .secondary-navigation
    %ul.wat-cf
      %li.first=link_to pat(:list), url(:esx_hosts, :index)
      %li.active=link_to pat(:new), url(:esx_hosts, :new)
  .content
    %h2.title
      =pat(:new)
      =mt(:esx_hosts)
    .inner
      -form_for :esx_hosts, url(:esx_hosts, :create), :class => :form do |f|
        =partial "esx_hosts/form", :locals => { :f => f }